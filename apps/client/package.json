{
  "name": "@danjocord/client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/",
    "sounds": "node scripts/gen-sounds.mjs",
    "sounds:measure": "node scripts/measure-sounds.mjs"
  },
  "dependencies": {
    "@danjocord/protocol": "workspace:*",
    "mediasoup-client": "^3.7.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
