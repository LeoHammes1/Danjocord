# deps / build
node_modules/
dist/
apps/server/client-dist/

# banco local (SQLite)
data/
*.db
*.db-wal
*.db-shm

# ambiente
.env
.env.*

# certificado de teste em rede local (scripts/lan-cert.mjs) — contém CHAVE
# PRIVADA, e é gerado por máquina/IP: nunca vai para o repositório
certs/

# artefatos de editor/SO
.DS_Store
Thumbs.db
apps/desktop/renderer-dist/
apps/desktop/release/

# rascunhos que agentes de auditoria escrevem no repo enquanto reproduzem um
# achado. Já entraram num commit por um `git add -A` distraído.
__audit_*
__refute_*
**/zz-*
